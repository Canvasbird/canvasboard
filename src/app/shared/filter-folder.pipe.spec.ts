import { FilterFolderPipe } from './filter-folder.pipe';

describe('FilterFolderPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFolderPipe();
    expect(pipe).toBeTruthy();
  });
});
